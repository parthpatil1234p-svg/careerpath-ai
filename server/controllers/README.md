# This folder holds controller files.
# Controllers contain the business logic that route handlers call.
# Keeping logic here (instead of in routes) keeps code clean and testable.
#
# Files added in Step 2+:
#   authController.js      — register, login, token generation
#   profileController.js   — get and update user profile
#   careerController.js    — fetch career data
#   recommendController.js — career-match scoring algorithm
#   roadmapController.js   — roadmap generation logic
#   progressController.js  — task completion updates
