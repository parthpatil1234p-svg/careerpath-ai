# This folder holds Mongoose schema files (database models).
# Each file defines the shape of a MongoDB collection's documents.
#
# Files added in Step 2+:
#   User.js          — user account, skills, and profile
#   Career.js        — career roles and required skills
#   Skill.js         — skill definitions and categories
#   Roadmap.js       — generated weekly learning plans
#   UserProgress.js  — task completion tracking per user
