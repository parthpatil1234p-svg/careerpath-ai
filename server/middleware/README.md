# This folder holds Express middleware functions.
# Middleware runs between a request arriving and the route handler executing.
#
# Files added in Step 2+:
#   authMiddleware.js    — verifies the JWT token on protected routes
#   errorMiddleware.js   — global error formatting (optional)
#   adminMiddleware.js   — checks if the user has admin role (optional)
