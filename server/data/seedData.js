/**
 * data/seedData.js — Aggregated Seed Data
 *
 * Exports skills and careers seed sets.
 */

const skillsData = require('./skillsData');
const careersData = require('./careersData');

module.exports = {
  skillsData,
  careersData,
};
