# This folder holds Express route files.
# Each file defines URL endpoints for a feature area.
#
# Files added in Step 2+:
#   authRoutes.js      — POST /api/auth/register, /api/auth/login
#   profileRoutes.js   — GET/PUT /api/profile
#   careerRoutes.js    — GET /api/careers
#   recommendRoutes.js — POST /api/recommend
#   roadmapRoutes.js   — POST/GET /api/roadmap
#   progressRoutes.js  — PUT /api/progress/:id
