# ============================================================
# CareerPath AI — Frontend Environment Example (Template Only)
# ============================================================
# IMPORTANT NOTE:
# This project uses plain HTML5, CSS3, and client-side JavaScript.
# Browsers execute code publicly; therefore, NEVER store private API
# keys, JWT secrets, database credentials, or sensitive secrets here.
#
# API configuration is resolved dynamically in `js/config.js`.
# If using a static site build-tool or environment injector in future:
# ============================================================

# Public backend API endpoint URL (no trailing slash)
# API_BASE_URL=https://YOUR-RENDER-SERVICE.onrender.com/api
